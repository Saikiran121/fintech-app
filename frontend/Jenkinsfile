pipeline {
    agent any 

    tools {
        nodejs 'nodejs-25'
    }

    stages{
        stage('npm version') {
            steps {
                sh 'node -v'
                sh 'npm -v'
            }
        }
    }
}